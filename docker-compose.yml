services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clinic-website
    image: nongdev/clinic-website:latest
    restart: always
    networks:
      - web

networks:
  web:
    external: true
  internal_project_network:
    driver: bridge